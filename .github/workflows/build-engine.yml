name: TayyibWord ARM64 Engine Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Check build host
        run: |
          uname -a
          uname -m
          nproc
          free -h
          df -h
